# Waveshare Bridge 环境变量

# 微雪 4.2" Cloud Module 设备 IP（AP 模式默认 192.168.4.1；连入路由后会是路由器分配的 IP）
WAVESHARE_DEVICE_IP=192.168.4.1
WAVESHARE_DEVICE_PORT=6868

# 桥接器 HTTP 控制端点
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=9000
